---
import { cn } from '@utils/cn';

interface Props {
  padding?: 'sm' | 'md' | 'lg';
  bg?: 'surface' | 'bg';
}

const { padding = 'md', bg = 'surface' } = Astro.props as Props;

const paddingSizes: Record<'sm' | 'md' | 'lg', string> = {
  sm: 'p-4',
  md: 'p-6',
  lg: 'p-8',
};

const bgColors: Record<'surface' | 'bg', string> = {
  surface: 'bg-surface',
  bg: 'bg-bg',
};

const classes = cn(
  bgColors[bg],
  'rounded-md shadow-sm',
  paddingSizes[padding]
);
---
<div class={classes}>
  <slot />
</div>